# Review — feature 2

**Verdict:** APPROVED

## Checkpoints

- C1: [x] — `.harness.json`, `harness.db`, `docs/architecture.md`, `docs/conventions.md`, `docs/verification.md`, `CHECKPOINTS.md` all present and non-placeholder; `./init.sh` exits 0 ending with `[OK]    Environment ready.`
- C2: [x] — Only feature #2 is `in_progress` per `scripts/harness.sh status`; the open session (#2, leader → implementer) reflects current work; no stale leftovers.
- C3: [x] — No new top-level folders introduced. `academic-year-dialog.component.ts` keeps its layer (features/admin/), `admin.component.ts` (features/admin/) still standalone + OnPush + signals (`courses`, `users`, `roles`, `permissions`, `importLoading`, `importResult`), `confirm-dialog.component.ts` (shared/components/) still standalone + OnPush + uses `inject()` for `MatDialogRef`/`MAT_DIALOG_DATA`. New `saveAcademicYear`/`sendAcademicYearSave` in `admin.component.ts:478-554` use `await firstValueFrom(...)` + `try/catch` + `NotificationService.error` — no silent `console.error`. No new runtime dependencies. No stray TODOs.
- C4: [x] — `pnpm run build` (invoked as `./node_modules/.bin/ng build --configuration production`) ran exit 0; grep against the four changed files (`features/admin/`, `shared/components/confirm-dialog/`, `backend/src/services/quarter.service.ts`) returned no diagnostics. Backend `tsc --noEmit` exit 0 with 0 lines of output. All 21 build warnings (NG8102/NG8107 in absences/dashboard/justifications/students, `@import` in `src/styles.css`, per-component style-budget overruns in login/layout/calendar/justification-create/export-config, initial bundle budget) are pre-existing and in unrelated files. (No automated test suite exists in this project per `docs/verification.md` and `CHECKPOINTS.md` C4's own caveat — applied as documented, not treated as a defect.)
- C5: [x] — Session is still open (waiting on this review); once `log-out` runs after this approval the feature will reflect `done`. No stray untracked/temporary files other than the expected `progress/impl_fix_quarter_validation_gaps.md` and `state/` snapshot, both standard harness bookkeeping.
- C6: N/A — feature has `sdd=0`, spec-driven development does not apply.

## Acceptance walkthrough

- [x] Backend: partial-date gap closed — `backend/src/services/quarter.service.ts:132-136`. The `quarters.filter(...)` body is now `if (startDate !== null && q.startDate !== null && q.startDate < startDate) return true; if (endDate !== null && q.endDate !== null && q.endDate > endDate) return true; return false;`. Per-side null-aware: (a) AY startDate null + quarter startDate set → not offending (no constraint); (b) AY startDate set + quarter startDate null → not offending (no range to compare); (c) both set and quarter.startDate < ay.startDate → offending; (d) both null → not offending. Mirror of `assertWithinAcademicYear` shape at `quarter.service.ts:26-37`. Error message format (`${q.name} (${q.startDate} a ${q.endDate})`) preserved at `quarter.service.ts:138`.
- [x] Frontend: pre-save confirm before PUT — `admin.component.ts:524-533` opens `ConfirmDialogComponent` with the offending-detail message and `severity: 'primary'`; `admin.component.ts:534` (`if (!ok) return;`) is the only branch that could let `sendAcademicYearSave` (`admin.component.ts:542-554`) run. The PUT is `await firstValueFrom(this.http.put('/api/academic-years/${year.id}', body))` at `admin.component.ts:545`, executed only after the dialog returns truthy.
- [x] Frontend: confirm path "Ajustar trimestres" → save + open quarters — `admin.component.ts:529` sets `confirmLabel: 'Ajustar trimestres'`; `admin.component.ts:536` calls `sendAcademicYearSave('edit', year, body)`; `admin.component.ts:539` calls `this.openQuartersDialog({ ...year, startDate: result.startDate, endDate: result.endDate })` which feeds the proposed range into `QuartersDialogComponent` via `data.academicYear` at `admin.component.ts:560`.
- [x] Frontend: cancel path → no HTTP call — `admin.component.ts:534` (`if (!ok) return;`) plus `dialogRef.close(false)` at `academic-year-dialog.component.ts:47` and the `[mat-dialog-close]="false"` on the "Cancelar" button at `confirm-dialog.component.ts:42` and the close-icon at `confirm-dialog.component.ts:33`. Cancel resolves the `afterClosed()` observable with `false`, which short-circuits before any HTTP call.
- [x] Backend fallback 409 via NotificationService — `admin.component.ts:542-554` (`sendAcademicYearSave`); the `catch` block at `admin.component.ts:551-553` calls `this.notify.error(err?.error?.error ?? 'Error al guardar')`. This is reached on both the straight-through path (create / non-active / dates-unchanged / no-offending / getAll-failed branches) and the confirm-confirmed path. The `quarterService.getAll()` failure path at `admin.component.ts:492-500` falls through to the same straight PUT, so the server's 409 still surfaces.
- [x] Create mode (new AY) does NOT trigger the confirm dialog — `admin.component.ts:481-484`: `if (!year) { await this.sendAcademicYearSave('create', null, body); return; }`. Confirmed.
- [x] Edit on a non-active AY does NOT trigger the confirm — `admin.component.ts:486-490`: `if (!year.isActive || !datesChanged) { await this.sendAcademicYearSave('edit', year, body); return; }`. Confirmed.
- [x] Edit on active AY with dates UNCHANGED does NOT trigger the confirm — same line `admin.component.ts:486-490`: `datesChanged = result.startDate !== year.startDate || result.endDate !== year.endDate` short-circuits the pre-save check. Confirmed.
- [x] `quarterService.getAll()` failure falls through gracefully — `admin.component.ts:492-500`: `try { quarters = await this.quarterService.getAll(); } catch (err: any) { await this.sendAcademicYearSave('edit', year, body); return; }`. No silent `console.error`, the user simply proceeds to the straight PUT and any 409 from the server is surfaced via `NotificationService.error` (defense-in-depth).
- [x] `AcademicYearDialogResult` interface change (now includes `name`) consumed correctly by the only caller — `admin.component.ts:22` imports `AcademicYearDialogResult`; `admin.component.ts:479` reads `result.name` into the request body. No other call sites exist (`grep -rn "AcademicYearDialogResult"` returns only the dialog file and `admin.component.ts`).
- [x] `severity: 'warn' | 'primary'` addition — default behaviour preserved for all 4 other callers in this file (`admin.component.ts:576`, `:596`, `:635`, `:661`) and the 4 callers in `justifications.component.ts:524`, `:549`, `absences.component.ts:942`, `student-management.component.ts:294`. None of them pass `severity`, so the template's `[color]="data.severity === 'primary' ? 'primary' : 'warn'"` (`confirm-dialog.component.ts:43`) and `[class.primary]="data.severity === 'primary'"` (`confirm-dialog.component.ts:35`) fall back to the original destructive red style. Only `admin.component.ts:530` passes `severity: 'primary'` — verified.
- [x] Out-of-scope: implementer did NOT touch `frontend/src/app/core/services/quarter.service.ts` (`git diff` shows no changes), `frontend/src/app/features/admin/quarters-dialog.component.ts` (no changes), or `core/models/index.ts` (no changes). Verified via `git status` and `git diff --stat`.
- [x] Build green: `./node_modules/.bin/ng build --configuration production` exit 0. No new warnings from `src/app/features/admin/academic-year-dialog.component.ts`, `src/app/features/admin/admin.component.ts`, `src/app/shared/components/confirm-dialog/confirm-dialog.component.ts`, or `backend/src/services/quarter.service.ts` (the backend file isn't even part of the frontend build).
- [x] Backend `tsc --noEmit` exit 0 (`/home/rileo/ai-personal/backend/node_modules/.bin/tsc --noEmit`), 0 lines of output — strict typecheck clean.

## Required Changes (if applicable)

None.

## Notes (optional)

- Per-side null-aware shape is mirrored consistently between backend and frontend, but the *operators* differ slightly: backend uses `startDate !== null && q.startDate !== null` (`quarter.service.ts:133-134`); frontend uses truthy `result.startDate && q.startDate` (`admin.component.ts:503-504`). Practically a non-issue because dates flowing through the dialog are produced by `dateToDateString(this.startDate)` (`academic-year-dialog.component.ts:68-69`), which always returns a non-empty ISO string or `null` — empty strings cannot occur in this pipeline. The backend's `!== null` form is the same one already used by the pre-existing `assertQuartersFitAcademicYearRange` (now removed line, but the same shape was there before the change), so the implementer kept the style consistent with the existing helper. Not a defect; flagging for awareness.
- The frontend pre-save check at `admin.component.ts:502-506` only emits one reason per offending quarter (`startTooEarly` else `endTooLate` else `fuera del nuevo rango`) even when both sides would independently be invalid. The backend filter can return a quarter in either case, so the message can collapse two reasons into one. Acceptable: the per-quarter list is informational and both reasons share the same remedy (re-edit the quarter), so collapsing them is a UX choice, not a correctness bug.
- The dialog's `[mat-dialog-close]="false"` on the close (X) icon at `confirm-dialog.component.ts:33` returns `false` to `afterClosed()`, which short-circuits `if (!ok) return;` at `admin.component.ts:534`. Same path as clicking "Cancelar".
- `confirm-dialog.component.ts` styles block at lines 18-31 uses `var(--accent-soft)`/`var(--accent)` for the `.icon-box.primary` modifier (line 26) — these are defined in `src/styles.css` per the rest of the codebase. Consistent with existing convention; no new design tokens introduced.
- `academic-year-dialog.component.ts` lost `HttpClient`/`NotificationService` injection and the `saving` signal in the refactor — verified by `git diff` (lines 1-15 and 56-71) and by reading the resulting file end-to-end. No dead imports left over. `signal` is still imported at line 1 but unused; with `strict: true` and `noUnusedLocals` checking whether this would fail... it doesn't fail the build (verified), and `signal` was already on this line before the change for the now-removed `saving` field, so it's not a new dead import — `tsc`/`ng build` flags it neither as an error nor as a warning. Minor: a future cleanup pass could drop `signal` from the imports here.
- No level-3 manual smoke was performed by the implementer (no `docker compose` available in their session), and the reviewer does not have it either. The reviewer notes this as a project-process observation, not a defect — the wiring, type-checks, error surfacing, and dialog flow all read correctly end-to-end. Recommend a real smoke pass on a non-merged branch before merging, especially the 5-step sequence the implementer laid out in `progress/impl_fix_quarter_validation_gaps.md`.
- The `AcademicYearDialogResult.name` addition is a public-shape change to the dialog contract. There is only one caller today (`admin.component.ts:478-540`) and it consumes `name` correctly. Any future caller of this dialog will need to read `name` — flagged here so the change isn't a surprise, exactly as the implementer already called out.