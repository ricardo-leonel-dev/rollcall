# Review — feature report_conflicting_absence_type_on_create

**Verdict:** APPROVED

## Checkpoints

- C1: [x]
- C2: [x]
- C3: [x]
- C4: [x]
- C5: [x] (review being recorded here — `record-review` to be run)
- C6: [x] (sdd=1)

## Verified against the spec

- **Spec adherence (R1–R9):** every requirement has a T-mapping and an observable artefact.
  R1/R2 (created/skipped semantics preserved) — case (iii) `created: 2`, case (iv) `created: 1` for restored row. R3 (same-type → `conflict: false`) — case (i-b) all three entries `conflict: false`. R4 (different-type → `conflict: true`) — case (ii) all three entries `conflict: true`, case (iii) 09-17 entry `conflict: true, existingType: "AT"`. R5 (`skippedDetails.length === skipped`) — every case matches. R6 (ascending order) — every array is strictly ascending. R7 (empty array not null) — cases (i-a) and the seeds in (iii) show `"skippedDetails":[]`. R8 (manual smoke test) — four cases captured verbatim. R9 (build + no new TS errors) — `node_modules/.bin/tsc -p .` exits 0; `./init.sh` ends `[OK] Environment ready`.
- **Exact code shape:** `git diff src/services/absence.service.ts` shows only the four changes from `design.md`'s "Exact code shape" section — widened `SELECT date::text AS date, type`, new `existingTypeByDate` Map, `existingDates` derived from `existingTypeByDate.keys()`, and the `skippedDetails` construction. Return type annotation and final return statement match verbatim.
- **Out-of-scope preservation:** `toCreate` filter, `softDeletedRows` query, `softDeletedByDate` Map, `toRestore`/`toInsert` split, and the transaction body are byte-identical to the pre-feature version. Restore path works (case iv: soft-deleted 09-15 was re-activated, `deleted_at` is NULL again, NOT in `skippedDetails`, counts toward `created`).
- **Controller untouched:** `git diff src/controllers/absence.controller.ts` is empty. `POST /` handler at line 23 forwards `svc.createRange(...)`'s resolved value as the 201 JSON body — no controller-level response shaping needed.
- **Conventions:** services own business logic; controllers stay thin; no new dependencies; raw SQL via `AppDataSource.query`; camelCase TS properties over snake_case DB columns; no superfluous comments.

## Verification ran

- `node_modules/.bin/tsc -p .` → `EXIT=0` (pnpm itself isn't installed in this environment; the underlying `tsc` binary was invoked directly, which is what `pnpm run build` calls per `package.json`).
- `./init.sh` → ends with `[OK] Environment ready`. Pre-existing `[WARN]`s for empty `verify_command` and unset `SUPABASE_URL` are unchanged from baseline and acceptable per `docs/verification.md`.
- Manual smoke transcript documented in `progress/impl_report_conflicting_absence_type_on_create.md` (cases i-iv) is internally consistent and demonstrates every R1–R8 requirement.

## Anti-over-reach check

Only `src/services/absence.service.ts` is modified for this feature. Untracked files within scope: `progress/impl_report_conflicting_absence_type_on_create.md` (allowed), `specs/report_conflicting_absence_type_on_create/` (spec files, allowed), `state/features/...` and `state/sessions/...` (generated by `init.sh`'s snapshot step, allowed). The pre-existing modifications in `../frontend/...` belong to feature 11, not this one — out of scope for this review.

## Non-blocking observation

`specs/report_conflicting_absence_type_on_create/tasks.md` still has every task as `[ ]`. The implementer's handoff doc (`progress/impl_*.md` T1–T5 narrative) explicitly walks through each task and confirms every "Done:" condition was met, which satisfies the CHECKPOINTS.md C6 escape clause ("any left `[ ]` has a documented, reviewer-accepted justification in `progress/impl_<feature>.md`"). However, ticking the boxes themselves is convention (feature 5's `tasks.md` has all `[x]`), so a cleanup tick before final close would be polite — not blocking acceptance.

## Required Changes

None.